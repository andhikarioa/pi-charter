{
  "name": "pi-charter",
  "version": "0.2.1",
  "private": true,
  "license": "MIT",
  "description": "Deterministic, fail-closed governance compiler for bounded Pi work: authority, scope, routing, capability truth, bounded instructions, and receipts.",
  "type": "module",
  "keywords": [
    "pi-package"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "!dist/**/*.test.js",
    "!dist/**/*.test.d.ts",
    "!dist/core/validation/fixtures.*",
    "extensions",
    "skills",
    "compiler-identity.json",
    "README.md"
  ],
  "pi": {
    "extensions": [
      "./extensions"
    ],
    "skills": [
      "./skills"
    ]
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node scripts/run-tests.mjs",
    "verify": "npm run build && npm run typecheck && npm test && npm pack --dry-run",
    "smoke:compiler": "node scripts/compiler-tamper-smoke.mjs",
    "smoke:pi": "node scripts/pi-integration-smoke.mjs",
    "smoke:consumer": "node scripts/consumer-smoke.mjs",
    "prepack": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^5.9.3"
  }
}
